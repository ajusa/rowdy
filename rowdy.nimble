version     = "0.1.0"
author      = "ajusa"
description = "Magically map procs to mummy's router"
license     = "MIT"

srcDir = "src"

requires "nim >= 1.6.8"
requires "webby >= 0.1.3"
requires "mummy >= 0.2.1"
