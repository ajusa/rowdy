version     = "0.1.0"
author      = "ajusa"
description = "Magically map procs to mummy's router"
license     = "MIT"

srcDir = "src"

requires "nim >= 1.6.8"
requires "urlly >= 1.1.0"
requires "mummy >= 0.2.1"
